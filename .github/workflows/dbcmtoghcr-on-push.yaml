---
name: dbcmetrics Docker Image on Push to GHCR

on:
  push:
    branches-ignore:
      - master
    paths:
      - 'dbcmetrics/**'

jobs:
  call-workflow:
    uses: ./infra-tools/.github/workflows/imagetoghcr-on-push.yaml
    with:
      image_name: "dbcmetrics"