---
name: BAP Docker Image on Tag

on:
  push:
    tags:
      - 'infra-tools-[0-9]+.[0-9]+.[0-9]+'
      - 'dbcmetrics-[0-9]+.[0-9]+.[0-9]+'
      - 'awx-ee-[0-9]+.[0-9]+.[0-9]+'

jobs:
  build_and_push:
    uses: hpi-schul-cloud/infra-tools/.github/workflows/bap.yaml
